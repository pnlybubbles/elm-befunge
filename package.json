{
  "license": "MIT",
  "name": "elm-befunge",
  "version": "1.0.0",
  "scripts": {
    "start": "elm-reactor --address=0.0.0.0",
    "build": "yes | elm-make index.elm --output public/index.html",
    "clean": "rm -rf public/*",
    "test": "echo 'All good!'"
  },
  "dependencies": {
    "elm": "^0.18.0"
  }
}
